spring:
  cloud:
    nacos:
      config:
        enabled: false
        username:
        password:
      discovery:
        server-addr:
        namespace:

smart:
  cloud:
    route:
      config:
        data-id:
        group:
        data-type: yml